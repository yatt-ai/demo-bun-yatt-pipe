{
  "name": "bun-calculator",
  "version": "1.0.0",
  "description": "Simple calculator demo with Bun testing",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "test:report": "bun test --coverage --reporter=json > test-results.json"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
